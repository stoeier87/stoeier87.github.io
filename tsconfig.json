{
  "$comment": "DECISIONS.md ADR-011: gradual now, strict later. TypeScript is installed and configured so it is ready the moment the React migration starts, but nothing is type-checked into submission today. TIGHTENING TRIGGER: flip strict and noUncheckedIndexedAccess in the first PR that adds a .ts file to shared/ -- not 'later', because later does not arrive.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": [],

    "allowJs": true,
    "checkJs": false,

    "strict": false,
    "noEmit": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["tokens.ts", "shared/**/*.ts", "proto/**/*.ts"],
  "exclude": ["node_modules", "dist", "public"]
}
